# MEMORY SEED

Jarvis has a persistent private SQLite memory restored between GitHub Actions runs. This Markdown file is only a public seed and policy reminder.

- Search private runtime memory when prior context may matter.
- Store durable project decisions and explicit remember requests in private runtime memory.
- Never write secrets or private personal history into this public repository file.
